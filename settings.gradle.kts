rootProject.name = "dynamically-image-loader"

pluginManagement {
    repositories {
        gradlePluginPortal()
        // PaperMC Repo
        maven("https://papermc.io/repo/repository/maven-public/")
    }
}

