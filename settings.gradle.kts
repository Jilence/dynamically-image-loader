rootProject.name = "kotlin-gradle-template"

pluginManagement {
    repositories {
        gradlePluginPortal()
        // PaperMC Repo
        maven("https://papermc.io/repo/repository/maven-public/")
    }
}

